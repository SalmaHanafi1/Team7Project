<h2>Comments</h2>
<% @article.comments.each do |comment| %>
  <p>
    <strong>Commenter:</strong>
    <%= comment.commenter %>
  </p>
<%= link_to ' Listing feedback', comments_path %>
  <p>
    <strong>Comment:</strong>
    <%= comment.body %>
  </p>
<% end %>
